#include "./instruction.h"
#include <bits/stdc++.h>
#include "../common.h"
using namespace std;

unordered_map<string, int> funcLabels;
const int STACK_BASE = 0;
int labelNum = 1;

string generate(Node* head) {
    // Loop through each function and generate a id for each 
}